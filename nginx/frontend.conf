server {
    listen 80;
    index index.html;
    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/matchbot/angular/dist/browser;

    location / {
        try_files $uri$args $uri$args/ /index.html;
        gzip_static on;
    }
}
